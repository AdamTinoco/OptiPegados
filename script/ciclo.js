function ciclo(actual,camino,flag)
{
    if(actual.fila == camino[0].fila && actual.columna == camino[0].columna && camino.length>1)
        return camino;
    if(flag==1)       //busqueda por fila
    {
        
    }
    
}